pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        repositories {
            maven { url "https://jitpack.io" }
        }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        repositories {
            maven { url "https://jitpack.io" }
        }
    }
}
rootProject.name = "ContactFinal"
include ':app'
